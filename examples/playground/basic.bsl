&НаКлиенте
Процедура ПоказатьТовары(Команда)
    Запрос = Новый Запрос;
    Запрос.Текст =
        "ВЫБРАТЬ
        |    Товар.Ссылка
        |ИЗ
        |    Справочник.Номенклатура КАК Товар";

    Результат = Запрос.Выполнить();
    Сообщить(Результат);
КонецПроцедуры
