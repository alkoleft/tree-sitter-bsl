# Функция УНИКАЛЬНЫЙИДЕНТИФИКАТОР(UUID)

```
УникальныйИдентификатор(<Выражение>)
```

Функция позволяет получить уникальный идентификатор переданной ссылки.

> **Выражение **- Выражение, результатом вычисления которого является ссылочное значение (кроме ссылки на таблицы внешних источников данных).

**Возвращаемое значение:
**Зависит от параметра функции:

- NULL - NULL.

- Неопределено либо пустая ссылка - пустой уникальный идентификатор (типа УникальныйИдентификатор).

- Для значения с типом ссылки - значение типа УникальныйИдентификатор.

- Для других типов (включая входящие в составной тип) - формируется ошибка времени выполнения.


> **см. также:** [Функции языка запросов](../../../../использование-выражений-в-языке-запросов/функции-языка-запросов/index.md)
